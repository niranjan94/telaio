import type { Command } from 'commander';

/** Registers the `telaio dev` CLI command. Implemented in a later phase. */
export function registerDevCommand(_program: Command): void {
  // Placeholder — full implementation follows in the next item.
}
